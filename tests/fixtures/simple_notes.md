# Document Pipeline Notes

This test document verifies the end-to-end upload flow.

The preview service should convert Markdown text into a multimodal manifest, generate Slide-JSON, and render the result in the browser.
