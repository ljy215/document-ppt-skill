interface:
  display_name: "ToPPT"
  short_description: "Turn documents into slide-ready JSON, HTML previews, and PPTX-ready assets"
  default_prompt: "Use $toppt to parse the named document into a multimodal manifest, generate strict Slide-JSON, create an Agent-authored HTML preview bundle when requested, and only produce a Chinese Markdown report for legacy report requests."
