interface:
  display_name: "Paper Reader"
  short_description: "Extract multimodal paper assets and prepare slide-ready intermediates"
  default_prompt: "Use $paper-reader to parse the named paper into a multimodal manifest, generate strict Slide-JSON when requested, and only produce a Chinese Markdown report for legacy report requests."
